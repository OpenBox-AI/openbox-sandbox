[package]
name = "openbox-sandbox-launcher"
version = "0.0.0"
edition = "2021"
publish = false
description = "Launcher that locates, pins, and runs externally-provided OpenBox and OpenShell artifacts."

[[bin]]
name = "osb"
path = "src/main.rs"

# Isolated from the parent crate: this launcher builds on its own.
[workspace]
